version: 2.1

orbs:
  php: circleci/php@1.1.0
  
jobs:
  build_all:
    docker:
      - image: 'cimg/base:stable'
    steps:
      - checkout
      - php/install-php:
          version: '8.3'
workflows:
  version: 2
  reproduce-apt:
    jobs:
      - build_all
