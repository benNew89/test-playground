version: 2.1

jobs:
  reproduce-apt-logs:
    docker:
      - image: ubuntu:22.04
    steps:
      - run:
          name: Update APT and Install systemd
          command: |
            apt-get update
            DEBIAN_FRONTEND=noninteractive apt-get install -y systemd
