version: 2.1

orbs:
  php: circleci/php@2.0.0
  
jobs:
  build_all:
    docker:
      - image: 'cimg/base:current'
    steps:
      - checkout
      - php/install_php:
          version: '8.3'
workflows:
  version: 2
  reproduce-apt:
    jobs:
      - build_all
